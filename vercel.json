{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run build && npm run build:server && node scripts/fix-server-imports.mjs",
  "outputDirectory": "dist",
  "functions": {
    "api/[[...path]].ts": {
      "includeFiles": "dist/server/**"
    }
  }
}